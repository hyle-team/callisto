table:
  name: bridge_tokens
  schema: public
select_permissions:
  - permission:
      allow_aggregations: false
      columns:
        - metadata_id
        - tokens_info_id
      filter: {}
      limit: 100
    role: anonymous